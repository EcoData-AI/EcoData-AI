"""GAIA's tool system — Milestone 2.

Calculator is the only tool that ships this milestone. See `base.py` for the
`Tool` contract and `registry.py` for the single place that knows what exists,
mirroring `gaia.llm` deliberately: same shape, same "must never raise" rule.
"""

from __future__ import annotations
